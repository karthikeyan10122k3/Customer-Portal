import { SalesItemNumberPipe } from './sales-item-number.pipe';

describe('SalesItemNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SalesItemNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
